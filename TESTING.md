# 0.3.0 验收记录

验收日期：2026-09-11。基于本地 0.2 增量开发，旧记录保留在本文后半部分。

## 本轮环境与执行

Windows，Node.js 24.18.0，npm 12.0.1，Playwright 1.63.0；Microsoft Edge 152.0.4191.66 独立无头进程。桌面 1440 × 1000；手机模拟 390 × 844、触屏与 DPR 2；另验证 360px 宽度。

| 检查                   | 结果                                                                          |
| ---------------------- | ----------------------------------------------------------------------------- |
| `npm test`             | 41 项通过：原有 32 项 + 新增 9 项                                             |
| `npm run test:e2e`     | 62 项通过：原有 42 项 + 新增 20 项；无失败、跳过或重试项                      |
| `npm run build`        | TypeScript 与 Vite 生产构建通过                                               |
| `npm run format:check` | 通过                                                                          |
| 生产包导出与浏览器重启 | 4 项通过，桌面 / 手机模拟各覆盖两布局导出与完整进程重启；无失败、跳过或重试项 |

```sh
npm test
npm run test:e2e
npm run build
npm run test:e2e -- --config playwright.production.config.ts
npm run format:check
```

开发测试报告：`.qa/results.json`、`.qa/report/index.html`；生产报告：`.qa/production-results.json`。生产测试只服务已构建的 `dist/`，使用独立的 `http://127.0.0.1:4175` 来源（4173 已有预览服务占用，本轮未停止该服务）。截图、实际下载 PNG、生成的测试图和专用浏览器资料目录均放在 `.qa/`，不上传 GitHub。

## 新增覆盖

| 类别     | 验证内容                                                                                                                               |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| 两种布局 | 上下照片条与写真拼贴，各含两个独立照片槽位；均导出 1800 × 2400 PNG，比例 3:4                                                           |
| 文案     | 姓名、80 字素短句；可选年月日、不显示日期；校验闰日与非法日期；超限仍保留输入并禁止导出                                                |
| 独立照片 | 分别上传不同颜色和比例的程序测试图；换任一照片不改变另一张的字节和裁切；换图撤销、重做恢复完整内容                                     |
| 布局记忆 | 两张照片在两个布局独立记忆位置与缩放；切回恢复原参数，刷新后继续恢复                                                                   |
| 操作     | 鼠标与 Chromium 真实触摸事件拖动；拼贴重叠处选中上层照片；按钮可选择底层；方向键只移动选中照片；一次拖动仅占一步历史                   |
| 我的物料 | 小卡与生日作品共存；复制后两个照片 ID 独立；重命名不改画面姓名；真实缩略图和确认删除                                                   |
| 保存事务 | 双 Blob 与内容同一事务提交；事务中止回滚；只换照片 2 也更新内容版本并拒绝旧缩略图                                                      |
| 保存状态 | 编辑立即使旧成功状态失效；延迟旧保存不会把新编辑标成已保存；待保存时请求离开提醒；进入物料列表前完成最终保存                           |
| 保存失败 | 配额失败显示原因，保留当前编辑和两图；可先导出，再重试保存；持久存储许可提示跟随允许 / 拒绝结果                                        |
| 兼容     | v2 生日记录不重写；v3 小卡缺图、错误引用、缺裁切、未知版本被拒绝；损坏小卡保留原记录，仍能创建独立作品；继续运行原有 v1 迁移与回滚测试 |
| 手机尺寸 | 360 / 390px 无横向溢出，照片切换、缩放、输入及导出可触达；旧生日编辑器的吸顶规则限定在原步骤界面                                       |

0.2 基线复验曾出现「撤销换图后还短暂显示上一版已保存」的失败；本轮通过同步使保存状态失效、核对提交内容版本修正，并加入延迟事务测试。重命名后的作品菜单会保留展开状态，测试按实际展开状态操作，避免再次点击将菜单收起。

## 完全退出浏览器后恢复

`tests/restart.spec.ts` 不使用页面刷新代替浏览器重启，执行以下过程：

1. 用本次测试专用资料目录启动独立 Edge 进程，在固定来源创建一份生日作品和一份小卡。
2. 上传两张不同测试图，修改两个布局的裁切、姓名、短句与日期，等待「已保存到本机」。读取两张 Blob 的 SHA-256、全部项目字段、作品 ID、时间和版本。
3. 关闭持久上下文以退出其唯一浏览器；检查浏览器连接已断开，并用系统进程存在性检查确认原 PID 已退出。
4. 用**同一资料目录和同一来源**启动新 Edge，确认 PID 不同；默认恢复上次小卡，逐项比较两图哈希、日期、全部布局裁切、名称、ID、修改时间和内容版本，旧生日作品也保留。
5. 确认本次编辑会话没有继承旧撤销历史，并比较重开前后的真实预览像素。相同尺寸下平均通道差必须低于 0.1 / 255；允许不同进程的光栅化微差，不用 PNG 编码字节完全相同代替像素检查。

测试报告中的 `restart-evidence` 附件记录浏览器版本、来源、两个 PID、退出确认、照片哈希与恢复的布局参数；`restart-preview-difference` 记录实际像素差。未访问或关闭用户个人浏览器资料。

## 真实导出效果

通过界面的下载按钮获取 PNG，验证 PNG 签名、1800 × 2400 尺寸、有效体积，并将实际文件缩到主预览 Canvas 大小比较像素，阈值 5 / 255。参考图取自实际界面，没有重新调用渲染器生成参考。

每种布局分别导出带日期与不带日期的成品；已查看照片条、相纸拼贴、长文本成品与桌面 / 手机截图。照片、姓名、短句和日期不越界；小卡不出现生日文案；选中边框与照片编号不进入 PNG。拼贴相纸的位置已下移，保留默认主写真眼部。测试中的人物为现有授权示例照片，姓名与日期为虚构示例。

开发全量回归中的平均通道差（/ 255，无日期 / 带日期）：

| 布局       |          桌面 |      手机模拟 |
| ---------- | ------------: | ------------: |
| 上下照片条 | 0.622 / 0.638 | 0.696 / 0.705 |
| 写真拼贴   | 0.557 / 0.568 | 0.656 / 0.664 |

40 字素姓名 + 80 字素短句在 360px 下的导出对照差为桌面 1.888、手机模拟 1.668，均低于 5。浏览器进程重启前后预览差为桌面 0、手机模拟 0.002888，均低于 0.1；照片 Blob 哈希和全部保存字段完全一致。

生产包独立复验的两布局像素差与上表一致；进程重启预览差同样为桌面 0、手机模拟 0.002888。生产重启证据：桌面原 PID 29408 已退出，新 PID 9596；手机模拟原 PID 7740 已退出，新 PID 23368；均恢复同资料、同来源下的两件作品与两张测试图片。

## 未完成的真机验证与边界

- **未使用实体 iPhone / Android，未进行 Safari / WebKit / Firefox 真机验证**；手机结果是 Windows Edge 上的视口、触屏及 DPR 模拟。系统软键盘、相册保存、长按下载、后台回收仍需目标设备复核。
- 浏览器重启验证使用独立自动化资料，已验证正常完全退出后的恢复；未模拟系统断电、OS 强制杀进程或长期磁盘回收。尚未保存的修改、清理网站数据、结束无痕会话都可能丢失作品。
- 持久存储允许 / 拒绝分支用模拟 API 验证提示逻辑，不保证某台真机必然批准。获准也不能防止主动清理。
- 本轮未发布线上站点；生产验证基于本机静态 `dist/`。GitHub 推送交付源代码；`dist/` 和测试产物不入库。
- 不支持多标签页协同、跨浏览器 / 资料 / 来源 / 设备同步。PNG 不含编辑工程或撤销记录。

# 0.2.0 历史验收记录

验收日期：2026-09-11。

## 环境与结果

- Windows，Node.js 24.18.0，npm 12.0.1。
- Microsoft Edge 152.0.4191.66，Playwright 1.63.0，独立无头浏览器会话。
- 桌面视口 1440 × 1000；手机视口 390 × 844，DPR 2，并启用移动设备及触屏模拟；另覆盖 360px 宽度。
- `npm test`：32 项通过，保留原有裁切、排版、历史与数据完整性覆盖，新增主题记忆和多作品事务测试。
- `npm run test:e2e`：42 项通过，无跳过、重试通过或失败项。原有 20 项适配作品入口与存储结构后通过，另增加 22 项桌面/手机模拟验证。
- `npm run build`：TypeScript 检查及 Vite 生产构建通过。
- 对 `dist/` 进行额外静态部署检查：挂载到 `/starloom/` 子目录后正常加载、编辑并下载两种 PNG；没有页面异常、404 或第三方网络请求。

## 已验证的行为

| 类别       | 用例与结果                                                                                                              |
| ---------- | ----------------------------------------------------------------------------------------------------------------------- |
| 完整流程   | 从选主题到上传、填写、配色、两份裁切和下载，桌面及手机模拟均完成                                                        |
| 长文本     | 40 字素中文及连续英文姓名、200 字素中文及连续英文祝福可排版；超过上限或手动换行过多时明确提示，保留输入并禁止导出       |
| 文字边界   | 组合音标、家庭 Emoji 按字素计数；空名字、无效生日和色值被校验                                                           |
| 生日       | 2 月 29 日可选；切换月份时将日期约束到有效范围                                                                          |
| 照片       | 方图、4:1 横图、1:4 竖图、透明 PNG、JPEG、WebP；极端 8000:1 / 1:8000 比例另有裁切单元测试                               |
| 方向信息   | 注入 EXIF Orientation=6 的 600 × 400 JPEG 被正确识别为 400 × 600                                                        |
| 大图与错误 | 5000px 长边在本地归一化为 4096px；损坏图片、HEIC、超过 30 MB 的文件均有提示，原作品保持可用                             |
| 裁切       | 头像与贺图分别保存位置及缩放，图像不会拖出空白区域；切换格式后能恢复各自参数                                            |
| 触屏       | 通过 Chromium 实际触摸事件验证拖动，缩放滑杆和主要按钮可触达；360 / 390px 无横向溢出                                    |
| 撤销重做   | 一次拖动合并为一步；换照片可完整撤销和重做；新修改清除重做分支；历史最多 50 步                                          |
| 草稿       | 刷新恢复上传照片、姓名、祝福、颜色、装饰及两份裁切；读取完成前不写入示例数据                                            |
| 存储异常   | 模拟配额错误后仍可编辑和导出；不兼容草稿不会被默认作品自动覆盖                                                          |
| 三套主题   | 六份独立构图真实导出；卡片沿用当前照片和文案；主题切换保留内容和应援色，可撤销、重做，刷新恢复各主题裁切                |
| 明暗配色   | 三主题分别验证黑、白、亮黄色应援色和最长中文；单元测试另覆盖红、绿、蓝、中性灰；深色正文和色块文字对比至少 4.5:1        |
| 多作品     | 新建和复制使用新 ID；复制后改姓名、主题不影响原作；作品管理名称独立于画面姓名；显示真实缩略图与修改时间                 |
| 删除       | 取消、确认删除、删除当前作品、删除最后一件作品及刷新后空列表；迟到保存不能重新创建已删除作品                            |
| 保存竞态   | 使用真实 IndexedDB 事务锁延迟保存，验证离开前完成保存、下一件作品不被串写；延迟图片解码，验证离开后的上传不污染下一会话 |
| 缩略图     | 仅接受当前内容版本的缩略图，迟到结果不能覆盖新版或恢复已删除记录；生成失败不影响内容保存                                |
| 历史隔离   | 新建、复制、重新打开其他作品后撤销为空；各作品的编辑内容和照片不共享可变状态                                            |
| 旧稿迁移   | 从真实版本 1 数据库升级，保留旧记录并逐字节对比照片；内容、主题裁切和保存时间完整保留；重复打开不重复迁移               |
| 迁移失败   | 中止迁移事务后作品和迁移标记均回滚，原数据保留；修复故障后可重试；损坏和未安装主题的旧稿不被覆盖                        |
| 数据库占用 | 另一标签页保持版本 1 数据库连接时，显示关闭其他页面的提示；释放占用后重试成功，原草稿完整                               |
| 导出       | 实际下载并解码 PNG，验证头像 1600 × 1600、贺图 2400 × 3000；安全区不进入成品                                            |
| 清晰度     | 查看原尺寸及长文本导出图，照片、字体和细线可见，文字区域没有相互覆盖或越界                                              |
| 隐私       | 制作与导出无照片 POST 请求，照片、字体、许可资源来自本站或浏览器 Blob                                                   |

## 预览与导出一致性

测试读取界面真实预览 Canvas 的图像，将实际下载的 PNG 缩至该预览尺寸后比较像素；未通过再次调用渲染器制造参考图。比较使用画布副本，避免反复读取生产 Canvas 像素而触发浏览器渲染后端切换。

| 测试环境 | 主题       | 头像平均通道绝对差 | 贺图平均通道绝对差 |
| -------- | ---------- | -----------------: | -----------------: |
| 桌面     | 生日来信   |        2.448 / 255 |        1.085 / 255 |
| 桌面     | 心动拍立得 |        1.349 / 255 |        0.668 / 255 |
| 桌面     | 此刻主场   |        1.952 / 255 |        1.126 / 255 |
| 手机模拟 | 生日来信   |        1.847 / 255 |        0.918 / 255 |
| 手机模拟 | 心动拍立得 |        1.144 / 255 |        0.691 / 255 |
| 手机模拟 | 此刻主场   |        1.364 / 255 |        1.227 / 255 |

验收阈值为 5 / 255，允许不同分辨率缩放、照片采样和字体抗锯齿产生的微小差异。两份作品的内容、裁切、换行、颜色与装饰一致。自动测试同时检查实际 PNG 的文件签名、像素尺寸及有效文件体积。

三主题矩阵使用页面上可见的主贺图与配套头像预览；原完整流程测试还覆盖格式切换后的头像主预览。头像预览像素尺寸不同，因此测量数值不应直接与首版表格比较。

「生日来信」另与修改前从旧生产界面导出的基准进行原尺寸比较：头像全部通道一致；贺图 28,800,000 个通道值中仅 116 个有 1～2 的差异，平均差为 0.000004132 / 255。旧布局与旧配色路径没有修改，该差异为极微小的绘图采样差别。

额外在纯静态 `/starloom/` 子目录部署中，用「此刻主场」及 40 字素姓名、200 字素祝福实际生成两张原尺寸成品；界面、字体、照片和许可文件无 404，无第三方请求或页面异常。

## 实际成品与复现

[三套成品对照页](docs/theme-comparison/index.html)和[复现说明](docs/theme-comparison/README.md)随项目分发，包含六张原尺寸 PNG。统一使用现有示例照片、林予安、08.16、默认两行祝福和 `#75866B`。三套构图并非同图换色，头像与贺图也并非同一布局缩放。

`docs/theme-comparison/manifest.json` 记录各 PNG 的尺寸、体积和 SHA-256，以及旧主题基准比较结果。`overview.png` 为这些真实文件组成的静态对照页截图。

## 复验与限制

可重复运行 `npm test`、`npm run test:e2e` 和 `npm run build`。浏览器测试报告在 `.qa/report/index.html`，机器可读结果在 `.qa/results.json`；该目录不随 Git 分发。开发环境中额外保留了 `.qa/final-desktop.png`、`.qa/final-mobile-editing.png`、长文本导出示例和 `.qa/production-report.json` 供本次交付核对。

尚未使用实体 iPhone / Android 手机，也未运行 Safari / WebKit 真机测试；系统软键盘、相册保存界面和移动端下载策略仍需在目标设备上复核。应用提供「查看原图并长按保存」作为手机保存入口。不同系统的 Emoji / 缺字回退可能有不同外观，不承诺跨设备逐像素相同。

本地存储无法保证在浏览器被立即强制结束或主动清除站点数据后保留尚未提交的修改；应以界面「已保存到本机」为准。现支持多作品，但不支持同一作品的多标签页协同编辑或跨设备同步。关闭编辑会话或刷新后不保留撤销历史。
